[pytest]

markers =
    live: DeepEval or Agent tests that call external Groq endpoints (requires GROQ_API_KEY).
    not_live: DeepEval or Agent tests that are deterministic and do not call external endpoints.

filterwarnings =
    ignore::pytest.PytestUnhandledThreadExceptionWarning